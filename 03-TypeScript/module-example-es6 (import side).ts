import { calculateAverage, DAYS_IN_MILLI, Airplane } from "./module-es6 airplain (export side)";
import AirplaneClass from "./module-es6 airplain (export side)";
import { calculateMin } from "./Ehab/test-export-file";
import { IntersectionType } from 'typescript'; // importing some type from some library (in this case the 'typescript' library that is installed on my project)

const average = calculateAverage([5,7,4,9,10]);

const example: IntersectionType = {};
console.log(example);

const airplane1 = new AirplaneClass(30, 3, 'lookheed martin', 2018, 80000);

